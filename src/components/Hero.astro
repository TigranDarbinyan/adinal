<section class="hero">
    <div class="overlay"></div>

    <div class="content">
        <h1>Personalized Laser Hair Removal in Scottsdale</h1>

        <p class="subtitle">Care by Inesa</p>
        <p class="subtitle">Professional • Licensed • Safe</p>

        <div class="cta">
            <a href="#services" class="btn-primary">Book Now</a>

            <a href="tel:+16028933883" class="btn-secondary">Call Now</a>
        </div>
    </div>
</section>


<style>
    .hero h1 {
        font-size: clamp(2.2rem, 5vw, 3.2rem);
        font-weight: 600;
    }

    .hero .subtitle {
        font-size: 1.1rem;
        margin-top: 1rem;
        color: #2f2f2f;
    }

    .hero {
        position: relative;
        min-height: 75vh;
        display: flex;
        align-items: center;
        background-image: url("/images/laser.png");
        background-size: cover;
        background-position: center;
        cursor: default;
    }

    .hero-content {
        position: relative;
        z-index: 2;
        max-width: 600px;
    }

    .hero h1 {
        font-family: 'Playfair Display', serif;
        font-size: clamp(2rem, 5vw, 3rem);
        line-height: 1.15;
        margin-bottom: 1.5rem;
    }

    .overlay {
        position: absolute;
        inset: 0;
        background: rgba(255, 255, 255, 0.82);
    }

    .hero-buttons {
        display: flex;
        gap: 1rem;
        margin-bottom: 1.8rem;
    }

    .hero-buttons a {
        padding: 0.9rem 1.8rem;
        font-size: 0.9rem;
        border-radius: 6px;
    }

    .btn-primary {
        background: #000;
        color: #fff;
    }

    .btn-secondary {
        background: transparent;
        border: 1px solid #000;
        color: #000;
    }

    .hero ul {
        list-style: none;
        padding: 0;
        margin: 0;
    }

    .hero ul li {
        font-size: 0.9rem;
        margin-bottom: 0.5rem;
        color: #2f2f2f;;
    }


    .content {
        position: relative;
        max-width: 740px;
        padding: 0 1.5rem;
    }

    .subtitle {
        margin-top: 1rem;
        font-size: 1.125rem;
        color: #2f2f2f;;
    }

    .cta {
        margin-top: 2rem;
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
    }

    .trust {
        margin-top: 2rem;
        padding-left: 1rem;
        color: #2f2f2f;;
    }
</style>
